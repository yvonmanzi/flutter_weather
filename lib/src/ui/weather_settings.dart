import 'package:flutter/material.dart';

class WeatherSettings extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(
          title: Text(':)'),
        ),
        body: Center(
          child: Text('i am settings'),
        ),
      ),
    );
  }
}
